{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "attach",
      "name": "handler",
      "program": "${workspaceFolder}/target/debug/handler",
      "preLaunchTask": "func: host start"
    }
  ]
}
